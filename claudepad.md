# Session Summaries

## 2026-06-19T~UTC - Per-level best scores + responsive layout
Simulation was solid (37→ no normal-play bugs found): empirically probed under-tested edges in a throwaway vm harness (climber+overhang, builder+ceiling, walk-off-edge cull, dig-through-thin-floor splat, two builders same tile, floater-onto-exit save, faller-onto-blocker) — all behaved correctly or only "broke" on contrived terrain that real levels prevent with steel walls (deleted the probe). So spent the budget on two genuine player-facing gaps. **(1) Per-level best score (the classic Lemmings feature this lacked).** New `lemmings.best` localStorage key (JSON `{levelIdx: saved}`); `recordBest()` only ever raises a level's best; `endLevel` records it and passes a `newBest` flag to `showResults`, which shows `Best: N/total` or a yellow `★ NEW BEST!`. Level-select shows `best N/total ✔` under each played level. Load is try/catch + range-validated (rejects non-JSON and impossible >total entries). **(2) Responsive layout — fixed a real clipping bug.** `#game-wrapper` was a hard `width:960px` with `body{overflow:hidden}`, so on any viewport <960px both edges (OUT counter, minimap, toolbar controls) were clipped with no way to scroll to them — confirmed via Playwright screenshot at 800px. Changed to `width:100%;max-width:960px` (desktop ≥960 unchanged; canvas is a 2:1 replaced element so it scales). Two breakpoints: ≤768px the ability bar takes its own full-width row (`flex-basis:100%`, dividers hidden) so all 8 buttons stay a tidy single row instead of being squeezed to a vertical column; ≤480px the header wraps + buttons shrink to 38px. **Verification:** vm suite 33→37 (best persists/only-improves, results readout + new-best flag, level-select readout, corrupt-store tolerance), all green. **Wet-tested in real Chromium (Playwright Python — the `playwright` CLI here is the python pkg, not node):** title/in-game fit with no clipping at 1280/800/600/375; toolbar 1 ability-row at 768/600/375; hard tests — real-browser localStorage round-trip survives a page reload (best 10/10 restored + shown), resize 1280↔320 mid-game never clips wrapper or toolbar, rapid P/F toggles stay consistent, zero console errors throughout. Updated README + ARCHITECTURE (persistence + responsive sections, test coverage).

## 2026-06-18T~UTC - Input/UX: nuke confirm + keyboard viewport scrolling
Repo was in good shape (28 green tests, no WIP), so closed two genuine input gaps rather than churn the solid simulation. **(1) Nuke now needs a confirming second press.** Previously a single `N` keypress *or* one click on the nuke button instantly armed every lemming as a bomber — an irreversible, multi-minute-level-losing footgun a keystroke away from other keys. Added `requestNuke()` (arms `nukeArmed` + lights the button on the first press, fires the real `startNuke()` on the second) and `cancelNukeArm()` (Esc / right-click disarm). Kept `startNuke()` as the unconditional trigger so the existing nuke simulation test still drives it directly. Reset paths covered: constructor, `loadLevel`, and `endLevel` (so an armed-but-unfired nuke can't bleed its lit button into the results screen or next level). **(2) Held-key viewport scrolling** (←/→ or A/D): keydown/keyup maintain a `scrollKeys` Set, `handleScroll()` (once per frame in `loop()`) applies it clamped to [0,W-VW], and a window `blur` clears the set so a key held across a tab switch can't scroll forever. The 1600px world previously only scrolled via mouse-edge or minimap-click. **Code review caught a real miss:** I migrated the `N` key to the confirm gate but left the toolbar button bound to `startNuke()` directly — so the button bypassed the confirm and contradicted its own "press twice" tooltip. Fixed (button → `requestNuke()`) and added a regression test pinning the button to the two-press flow. Tests 28 → 33 (nuke arm/confirm/cancel, keyboard-N + Esc cancel, button confirm, held-key scroll). All green; extracted script passes `node --check`. No browser wet test: no Playwright/Chrome available and the project is deliberately zero-dependency, but the headless vm suite drives the *real* keydown/keyup handlers and `handleScroll`/`requestNuke`/`cancelNukeArm` methods (not reimplementations), which is this repo's documented verification path. Updated README controls + ARCHITECTURE (simulation/rendering/tests sections).

## 2026-06-17T~UTC - Solvability tests: every level now has an end-to-end bot
Closed the biggest remaining test gap: only level 1 had a solve test, so a physics/geometry change could silently make levels 2–5 unwinnable and nothing would catch it. Empirically explored each level (ASCII terrain dumps + traced bots in a throwaway harness, since deleted) to confirm every level is actually beatable, then encoded the working strategies as deterministic tests. Added 4 tests (24 → 28): **L2 "Bridge the Gap"** — one lemming chains Builders across the bottomless gap (release rate dropped to 10 so the crowd doesn't outrun the bridge); saves 12/15 (need 10). **L3 "Bash Street"** — assign a Basher at each of the 3 walls, guarded so a piled-up crowd doesn't burn every charge on one wall (only one basher per wall region at a time); saves 20/20 (need 15). **L4 "Over the Top"** — climber+floater on everyone, plus one lemming bridges the bottomless pit from the wall top so floaters land on the exit platform instead of in the pit; saves 18/20 (need 16). **L5 "The Gauntlet"** — asserts the full chain is *traversable*: one lemming bashes a wall, builds over a gap, mines through a mass, then climbs the final wall and floats to the exit (full 22/30 quota also needs crowd management; the test proves the route connects, which is what guards against geometry breakage). Confirmed all bots are robust to the 1px random grass (identical save counts across 12+ runs each; suite green across 5 full runs). Key engine finding while exploring: an apparent 110px "fall" on L5 that survived is actually two chained sub-MAX_FALL falls (60px then 50px) by design — no bug. Also found L5's landing→exit drop is ~70px (just over MAX_FALL=62), so the masses need either the 5 floaters or a dig-down-then-bash-low terrain trick (40px drop) — noted as the level's difficulty crux, not changed. Tests are pure simulation (headless vm suite is the right verification; no rendering/timing), updated README + ARCHITECTURE test sections.

## 2026-06-17T~UTC - Bugfix: builder no longer destroys steel; miner/blocker test coverage
Found and fixed a real invariant violation: `addTerrain` (the only non-destructive terrain mutation, used by the builder to lay bricks) set the collision cell to type 1 *unconditionally*, so a builder building on or across steel silently converted indestructible steel into diggable earth. Every destructive path (basher/digger/miner/bomber) already routed through `removeTerrain` + `isSteel` guards and respected steel; the builder was the lone leak, and it meant a steel containment wall/floor built upon could later be breached by a digger/basher/bomber — contradicting ARCHITECTURE's "steel survives everything." One-line guard added to `addTerrain` (skip cells of type 2). Added a regression test (builder over a steel floor leaves the steel intact and still lays the bridge above it — confirmed it FAILS on the pre-fix code and passes after). Also closed two coverage gaps: the **miner** was the only skill with no physics test at all (added one asserting a true diagonal cut — descends AND advances — that stops at steel without removing it), and added a test that a **blocker** dropped when its footing is dug out transitions to falling (`doBlock`'s fall branch). Tests 21 → 24, all pass. Verified the fix is logic-only (pure simulation, fully exercised by the vm-based headless suite — no rendering/timing involved, so the headless suite is the right verification; not a visual change needing a Playwright pass). Updated ARCHITECTURE.md (terrain-model + test-coverage descriptions).

## 2026-06-17T~UTC - Maintenance: frame-rate-independent loop, UX fixes, more tests
Fixed the main loop to be genuinely refresh-rate independent. The old `loop()` ran at most one sim tick per rAF frame (3 in fast mode) and used a `dt%16` hack, so on sub-60fps displays the simulation silently ran slow — contradicting ARCHITECTURE's "monitor-independent" claim. Extracted a unit-testable `stepSim(dt)` that accumulates real elapsed ms and runs the correct number of fixed 60 Hz ticks, clamping gaps >250ms so a backgrounded tab can't trigger a catch-up spiral. Wet-tested in headless Chrome (Playwright): clock advanced exactly 120 ticks in 2.0s = 60Hz on the nose, lemmings spawn, digging/restart/fast-mode all work, zero console errors. UX fixes: a selected ability now auto-deselects when its last charge is spent (was left highlighted-but-unusable); clicking a lemming mid-splat no longer wastes a charge (handleClick/findHoverLem skip `S.SPLAT`); added `R` to restart the current level. Tests: 16 → 21 (added stepSim, per-level config sanity, auto-deselect, splat-click guard, R restart). All pass.

## 2026-06-11T~UTC - Maintenance: physics fix, nuke fix, timer UI, keyboard, persistence, test suite, docs
Fixed walker footing scan to be nearest-first (STEP_SCAN) — old top-down scan teleported lemmings up through bridges/overhangs within 6px overhead. Fixed nuke: flag now latches (previously auto-cleared once all current lemmings armed, reopening the entrance and leaving the level unfinishable); entrance closes during nuke and level ends when everyone is gone. Added visible TIME display (was invisible — levels ended by surprise), red under 30s. Added keyboard shortcuts (1-8/P/Space/F/N/M/Esc/-/=), right-click deselect, mute button, localStorage persistence of unlocked levels. Refactored button handlers into Game methods. Built zero-dep test suite (tests/run-tests.mjs): vm sandbox + DOM stubs, 16 tests incl. scripted solve of level 1 and synthetic keydown dispatch. Ran multi-agent code review; applied fixes: keydown ignores browser shortcuts (Cmd/Ctrl/Alt, but Ctrl+Alt=AltGr passes), toolbar buttons blur on click (Space won't re-trigger them), right-click deselect on minimap too, removed dead doWalk branch + unused countDead/maxTime, SAVE_KEY const, sfx.unlock() helper, s-time only rewritten when the displayed second changes. Wrote README.md + ARCHITECTURE.md.

## 2026-02-12T~UTC - Continuation: Bug fixes, code review, git init
Continued from compacted session. Applied remaining code review fixes: release rate boundary clamp (max 99), minimap ImageData pre-allocation (reuse instead of creating each frame). Previously applied 11 critical/major fixes from comprehensive code review (48 issues found). Initialized git repo, created GitHub remote (discordwell/lemmings), committed and pushed.

## 2026-02-12T~UTC - Initial build and performance fixes
Built complete Lemmings browser game as single HTML file (~1100 lines). V1 had severe perf issues (~4fps from per-pixel fillRect). V2 rewrote terrain gen and minimap to use ImageData arrays. Fixed Chrome background tab throttling (rAF suppressed). Fixed level design issues: added steel containment walls, redesigned layer thicknesses for MAX_FALL=62 compliance, fixed exit positions, fixed IN counter formula.

# Key Findings

- **Chrome rAF throttling**: requestAnimationFrame callbacks are completely suppressed for background tabs (Chrome extension sidepanel steals focus). setTimeout is also throttled to ~5fps. Game works fine when user opens tab directly.
- **MAX_FALL=62**: All level designs must ensure max continuous fall (through dug shafts + gaps) stays under 62px or lemmings splat.
- **Level design pattern**: Use 30px earth layers with 25px gaps for diggable terrain. Steel containment walls on sides prevent walk-off deaths. Steel floors catch lemmings after final fall.
- **ImageData perf**: Use ImageData pixel arrays for bulk terrain generation and minimap rendering. Pre-allocate and reuse ImageData objects.
- **Git remote**: github.com/discordwell/lemmings (public repo)
- **Tests**: `node tests/run-tests.mjs` — extracts the script from index.html into a Node `vm` sandbox with DOM/canvas stubs and drives `game.update()` directly. Any physics/rules change needs a test there. Use blank terrain + `fill()` for determinism (level generators add random grass blades).
- **Wet testing IS possible here** (corrects the 2026-06-18 "no Playwright" note): the `playwright` on PATH is the **Python** package (`/Users/discordwell/.green2blue/bin/playwright`, chromium installed). Drive it with `/Users/discordwell/.green2blue/bin/python3.12` + `from playwright.sync_api import sync_playwright`. Serve via `python3 -m http.server` (local only) and use `page.evaluate("game.<method>()")` to jump straight to results/level-select/specific levels for UI screenshots — far faster than scripting actual play. The CLI `playwright screenshot <url> <file>` is handy for one-shot static screens.
- **Walker footing scan**: must search nearest-current-height first (STEP_SCAN order). Scanning top-down (-6..+3) makes lemmings pop up through any solid ≤6px overhead (e.g. builder bridges).
- **Aligned holes chain falls**: digger holes at the same x in stacked layers create one continuous fall that can exceed MAX_FALL — stagger hole positions (level-1 solver test digs at 440/480/520).
